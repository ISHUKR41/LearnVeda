# Wallet Route

This folder owns the authenticated `/wallet` page.

## Files

- `page.tsx` renders Stars balance, earning rules, level milestones, and safety rules.
- `Wallet.module.css` contains wallet-specific layout and responsive styles.

## Notes

Stars are non-withdrawable learning credits. Ledger writes, fraud checks, and earning calculations should be implemented in backend services before production wallet transactions are enabled.
