# Features Route

This folder owns the public `/features` page.

## Files

- `page.tsx` explains EduQuest feature areas such as learning plans, battles, events, and progress tracking.
- `Features.module.css` contains all feature-page styling.

## Notes

Use this route for product capability explanations. Do not place authenticated dashboard widgets or database queries here unless the page is deliberately converted into a server-data page.
