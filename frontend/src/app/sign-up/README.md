# Sign Up Route

This folder owns the `/sign-up` registration page.

## Files

- `page.tsx` renders account creation fields, validation, and registration API submission.
- `SignUp.module.css` contains sign-up page layout and form styles.

## Notes

User creation, password hashing, duplicate-email checks, rate limits, and audit logging must remain in backend auth services and API routes.
